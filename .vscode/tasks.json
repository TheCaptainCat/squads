{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile extension",
      "type": "shell",
      "command": "npm run compile",
      "options": { "cwd": "${workspaceFolder}/clients/vscode" },
      "problemMatcher": ["$tsc"],
      "presentation": { "reveal": "silent", "panel": "dedicated" },
      "group": "build"
    }
  ]
}
