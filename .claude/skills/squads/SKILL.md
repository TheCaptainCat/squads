---
name: squads
description: "How to track work on this project with the squads (`sq`) CLI: create/transition items, comment, link context. Use whenever you start, hand off, or update work."
---

This is a squads-managed skill. Load and follow its full definition with:

`sq skill squads show`

Track related work with the `sq` CLI. Never edit the `<!-- sq:* -->` marker lines in managed files.

This pointer file itself is regenerated by `sq sync` — hand-edits here are overwritten on the
next sync; run the command above for the current definition instead.
