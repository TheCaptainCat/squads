"""Bundled TOML data: the workflow spec, playbook, and role catalog shipped with squads.

Purely a data package — no code beyond this docstring. Loaders in ``_workflow``,
``_interactions``, and ``_roles`` read these files via ``importlib.resources``.
"""
