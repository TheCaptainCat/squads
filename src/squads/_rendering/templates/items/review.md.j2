<!-- sq:body -->
## Scope

_TODO: what is under review?_
{% if extra.get('target_ref') %}

**Target:** `{{ extra.get('target_ref') }}`
{% endif %}
<!-- sq:body:end -->

{% set finding_kind = spec.item_subentity_kind(item.type) %}
{% set tag = spec.subentity_plural(finding_kind) %}
{% set field_code = primary_field_code(finding_kind, spec) %}
## {{ spec.subentity_container_heading(finding_kind) }}

_{{ field_label(finding_kind, field_code, spec) }}:_ {{ collection_legend(resolve_collection(finding_kind, field_code, spec), spec) }}

_Add with `sq review {{ item.id | idnum }} add-{{ finding_kind }} "…" --{{ field_code }} {{ field_default(finding_kind, field_code, spec) }}`; track with `sq review {{ item.id | idnum }} {{ finding_kind }} <n> update --status <Status>`._

{{ tag | open_marker }}
{{ tag | close_marker }}

## Discussion

<!-- sq:discussion -->
<!-- sq:discussion:end -->
