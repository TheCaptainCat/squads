# {{ title }} items

<!-- squads:managed — regenerated by `sq sync`; do not edit by hand. -->
<!-- squads:version:{{ version }} -->

{{ overview }}

**Lifecycle:** {{ lifecycle }}

## Commands

```bash
{% for c in commands %}
{{ c }}
{% endfor %}
```
{% for sec in sections %}

## For {{ sec.title }}

{{ sec.text }}
{% endfor %}

---
The `.md` frontmatter is the source of truth. Write prose directly in the body between the
`<!-- sq:* -->` markers; never edit the marker lines.
