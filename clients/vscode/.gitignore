out/
node_modules/
*.vsix
coverage/
.vscode-test/
# Vendored mermaid webview asset (scripts/copy-mermaid.js) — a build product like out/, not
# committed; reproducible from the pinned mermaid devDependency.
media/
