"""Meta layer: repo/package self-tests — asserts on the repository or built package itself
(ticket-ID hygiene, the bundled-docs registry, ships-in-wheel packaging, template-manifest
freshness), orthogonal to the unit->service->cli->integration behavioral axis.

See tests/CONVENTIONS.md for the full layer taxonomy.
"""
