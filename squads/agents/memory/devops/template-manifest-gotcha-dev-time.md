---
summary: 'Template manifest gotcha: dev-time regens corrupt the last release''s entry
  (keyed by version). At release, git-checkout the prior entry from its tag, then
  bump the version and regen.'
created_at: '2026-07-15T13:23:06Z'
---
Template manifest gotcha: dev-time regens corrupt the last release's entry (keyed by version). At release, git-checkout the prior entry from its tag, then bump the version and regen.