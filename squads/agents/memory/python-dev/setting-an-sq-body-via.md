---
summary: 'Setting an sq body via --file: pass raw markdown only, never wrap it in
  a content tag. A stray wrapper leaks a closing tag into the stored body and still
  passes sq check.'
created_at: '2026-07-15T13:22:55Z'
---
Setting an sq body via --file: pass raw markdown only, never wrap it in a content tag. A stray wrapper leaks a closing tag into the stored body and still passes sq check.