---
summary: The slow scale tests are skipped by default. Run the suite once redirected
  to a file and read that; use --lf, -x, or -k while iterating. Never re-run the full
  ~4-minute suite just to reslice its output.
created_at: '2026-07-15T13:22:53Z'
---
The slow scale tests are skipped by default. Run the suite once redirected to a file and read that; use --lf, -x, or -k while iterating. Never re-run the full ~4-minute suite just to reslice its output.