---
author: manager
posted_at: '2026-09-01T13:13:11Z'
until: '2026-11-01T00:00:00Z'
---
0.14 release sequence — the plan of record. Check here before starting or dispatching 0.14 work. Replaces the earlier sequence notice, which had gone false in three places.

Hard ordering rule (ADR-781 §6): the version bump comes BEFORE any template-manifest regeneration. Regenerating against a shipped release's manifest entry corrupts it. Only the 0.14.0 entry may move.

Ground truth as of this notice: working version 0.14.0, schema 0.14, one runner for the whole release (0.11 -> 0.14). v0.13.1 is tagged and shipped.

Done: FEAT-321 (contract/PRD type), FEAT-644, FEAT-693 (derived views), FEAT-790 (ref kinds), FEAT-791 (override uniformity), FEAT-792 (pointers).

Remaining, serial by operator instruction:
- FEAT-694 (the collapse) is the only feature still open. Its role half and skill half have landed; the corpus strip is what is left.
- The corpus strip does NOT fold into the migration runner. That was the earlier plan and it cannot work: this repo already reads schema 0.14, so `sq migrate up` answers "nothing to migrate", and `adopt` reaches the current stamp with no runner ever visiting the corpus. The architect ruled the strip onto Service.repair()'s corpus walk — no second Migration record, no schema change, no bump. It covers both retired region families: 632 files carrying sq:summary and 436 carrying sq:<kind>:<local-id>:head.
- Then the follow-up trio (narration/false prose, ambient-reset guard, nested unknown-key refusal), then the dedicated review of the strip, then the capstone task closes.

FEAT-690 is Draft and is NOT in 0.14 unless the operator says otherwise.

Standing correction from this release: prose that asserts something the code does not hold has cost real money three times — a false docstring reached test fixtures and goldens, a test asserted a fallback that never existed and passed by reading a stale file, and two refusals named a remedy that no longer applied. Verify a claim before writing it, and verify the replacement too.