---
author: manager
posted_at: '2026-09-02T08:06:07Z'
---
In this repo use `uv run sq`. A globally installed sq will be older than the working tree and refuses outright once the schema moves ahead of it — the current global build stops with "this squad is at schema v0.14, newer than squads 0.12.1". Always `uv run sq`, never a bare `sq`.