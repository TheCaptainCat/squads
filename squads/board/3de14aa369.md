---
author: manager
posted_at: '2026-09-02T08:08:17Z'
until: '2026-11-01T00:00:00Z'
---
0.14 release sequence — the plan of record. Check here before starting or dispatching 0.14 work.

Hard ordering rule (ADR-781 §6): the version bump comes BEFORE any template-manifest regeneration. Regenerating against a shipped release's manifest entry corrupts it. Only the 0.14.0 entry may move.

Ground truth: working version 0.14.0, schema 0.14, one runner for the whole release (0.11 -> 0.14). v0.13.1 is tagged and shipped.

Done: FEAT-321 (contract/PRD type), FEAT-644, FEAT-693 (derived views), FEAT-790 (ref kinds), FEAT-791 (override uniformity), FEAT-792 (pointers).

Remaining, serial by operator instruction:
- FEAT-694 (the collapse) is the only large feature still open. Its role half and skill half have landed; the corpus strip is what is left.
- The corpus strip does NOT fold into the migration runner. This repo already reads schema 0.14, so `sq migrate up` answers "nothing to migrate", and `adopt` reaches the current stamp with no runner ever visiting the corpus. The architect ruled the strip onto Service.repair()'s corpus walk — no second Migration record, no schema change, no bump. It covers both retired region families: 632 files carrying sq:summary and 436 carrying sq:<kind>:<local-id>:head.
- The parent-cycle defect is in scope by operator ruling: a self-parent is accepted silently and hangs every sq tree call squad-wide.
- Then the remaining follow-ups, then the dedicated review of the strip, then the capstone task closes.

FEAT-690 is NOT in 0.14. It briefly carried a targets ref to this milestone because the operator added it during scoping; he has since moved it, and its membership now points at the 0.15 milestone. Do not re-add it here.

Standing correction from this release: prose that asserts something the code does not hold has cost real money five times now — a false docstring that reached test fixtures and goldens, a test asserting a fallback that never existed which passed by reading a stale file, two refusals naming a remedy that no longer applied, and one overstated guard claim that had been copied into a second file. Verify a claim before writing it, and verify the replacement too.