---
author: op-pierre
posted_at: '2026-07-29T16:46:45Z'
---
Second pre-close sweep for 0.13, to run AFTER the ADR audit (notice above) because it needs that audit's map of which decisions are in force. Hunts bundled assumptions in code: status/type/slug literals outside the spec layer, hardcoded fallbacks, naming conventions standing in for declarations, module singletons off bundled inputs plus unused per-spec seams, custom_* parallel paths, kind-blind ref operations, derived data stored behind a guard exemption. Scope src/ and clients/; seven read-only readers sharded by layer, one writer. Every finding classified real gap / sanctioned by an in-force decision / cosmetic — and each real gap that gets fixed earns a tests/meta guard so it cannot return.