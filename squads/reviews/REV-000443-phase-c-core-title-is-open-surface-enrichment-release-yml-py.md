---
id: REV-443
sequence_id: 443
type: review
title: 'Phase C: core title/is_open surface enrichment + release.yml + Python CI path-scope'
status: Approved
author: reviewer
refs:
- TASK-439:addresses
- TASK-433:addresses
- TASK-442:addresses
subentities:
- local_id: F1
  title: sq mine --json emits the item shape without the new is_open key
  status: Fixed
  severity: low
- local_id: F2
  title: release.yml pins softprops/action-gh-release to mutable @v2 under contents:write
  status: Fixed
  severity: low
- local_id: F3
  title: 'No .vscodeignore: the VSIX will bundle src/, test/ and fixtures'
  status: Fixed
  severity: low
created_at: '2026-07-17T07:58:59Z'
updated_at: '2026-07-17T11:38:08Z'
---
<!-- sq:body -->
Round-3 review of the Phase C batch: TASK-439 (core title+is_open surface enrichment, Elias — reviewer-gated on additive-only), TASK-433 (release.yml VSIX release asset, Hugo), TASK-442 (Python CI path-scoping, Hugo). Scope: src/squads/_cli/_main.py + touched goldens + CHANGELOG (439); .github/workflows/release.yml (433); .github/workflows/test.yml (442). Concurrent unrelated work ignored.

Gates run by the reviewer: scoped pytest (-k 'tree or list or json or golden') + tests/meta green; pyright + ruff check/format clean on _main.py; both workflows parse as YAML; sq check clean. (Full Python suite already green per main loop.)

VERDICT BY TASK:
- TASK-439 (core enrichment): APPROVE. Additive-only + spec-driven confirmed (details below). One low surface-consistency note (F1).
- TASK-433 (release.yml): APPROVE. Tag-only, unified version from core, VSIX-as-asset, no marketplace/publish, no self-triggering release. Two low hygiene nits (F2, F3).
- TASK-442 (test.yml): APPROVE, no findings. paths-ignore correct on push+PR; required-checks/branch-protection caveat genuinely flagged (main has no required checks today), corrects the prior stale claim. Cleanly closes REV-438 F3.

ADDITIVE-ONLY CONFIRMATION (439): byte-diff of tree.json/tree_feat.json/list.json/list_feature.json shows ONLY new keys added — 'title'+'is_open' on tree nodes, 'is_open' on list items (id line gains a trailing comma, new key appended). Nothing renamed/removed/retyped; the frozen 1.0 read surface is preserved. override_list.json correctly left untouched — it is generated by 'sq override list --json' (a different command: tests/cli/test_json_output_shape.py:117), not 'sq list --json'.

SPEC-DRIVEN CONFIRMATION (439): is_open derives from get_active_spec().is_open(status) = 'not self.statuses[status].terminal' (_workflow/_models.py:779) — it reads the active spec's per-status terminal flag, NOT a hardcoded status list, so a renamed/added/overridden status stays correct with no code edit. Live repro (sq list --all --json): Done/Accepted -> is_open:false (TASK-428, TASK-434, ADR-427), Ready/Draft -> true (FEAT-100, TASK-440); notably Fixed(BUG-183) -> true because Fixed is not terminal for bugs in the spec — proving it is not a naive label match. Both surfaces + all depths: list --json (line 410) and tree --json (line 513) both enriched; sq tree EPIC-99 --json carries title+is_open at root and every child depth (verified to L3). Only one list --json path and one tree --json path exist. Conventions: no ticket/ADR IDs in src (meta gate green), tree docstring updated (list docstring never enumerated keys — consistent), CHANGELOG additive note in the 0.10 section, adopter-facing with no internal refs.

RELEASE-RISK (433): NO self-triggering publish/release. release.yml fires ONLY on a human-pushed v* tag; it builds the VSIX and creates/attaches it to the GitHub Release for that tag (softprops/action-gh-release) — it does NOT auto-tag, and there is NO marketplace/Open VSX publish step. The human tag push is the release-owner gate, consistent with release ownership. Unified version injected from core pyproject.toml via tomllib into 'npm version', not the placeholder package.json 0.9.0. npm run compile + main (out/extension.js) both exist.

Overall: APPROVE all three. No blocking findings — three low nits, all follow-up-able.
<!-- sq:body:end -->

## Findings

_Severity:_ 🔴 critical · 🟠 high · 🟡 medium · 🟢 low · 🔵 info

_Add with `sq review 443 add-finding "…" --severity medium`; track with `sq review 443 finding <n> update --status <Status>`._

<!-- sq:summary -->
| Finding | Severity | Status | Assignee | Title |
| --- | --- | --- | --- | --- |
| F1 | 🟢 low | Fixed |  | sq mine --json emits the item shape without the new is_open key |
| F2 | 🟢 low | Fixed |  | release.yml pins softprops/action-gh-release to mutable @v2 under contents:write |
| F3 | 🟢 low | Fixed |  | No .vscodeignore: the VSIX will bundle src/, test/ and fixtures |
<!-- sq:summary:end -->

<!-- sq:findings -->

<!-- sq:finding:F1 -->
### F1 — sq mine --json emits the item shape without the new is_open key

<!-- sq:finding:F1:head -->
**Status:** 🟡 Fixed
**Severity:** 🟢 Low
<!-- sq:finding:F1:head:end -->

<!-- sq:finding:F1:body -->
TASK-439 enriched sq list --json (line 410) and sq tree --json (line 513), both correct. But sq mine --json (_main.py line 886) emits the same Item shape via [i.model_dump(mode='json') for i in items] WITHOUT is_open — the one sibling item-list JSON surface left un-enriched.

Out of TASK-439's stated scope (list + tree only), and the VS Code client does not consume sq mine, so NOT a blocker or a 439 defect. Recording as a surface-consistency note: a future sq mine --json consumer would find the asymmetry surprising (mine items carry every other item key but not is_open). If mine --json is considered part of the enriched read surface, add the same spec-driven is_open there for parity; otherwise fine to leave as an explicit scoping decision.
<!-- sq:finding:F1:body:end -->

#### Discussion

<!-- sq:finding:F1:discussion -->
<!-- sq:finding:F1:discussion:end -->
<!-- sq:finding:F1:end -->

<!-- sq:finding:F2 -->
### F2 — release.yml pins softprops/action-gh-release to mutable @v2 under contents:write

<!-- sq:finding:F2:head -->
**Status:** 🟡 Fixed
**Severity:** 🟢 Low
<!-- sq:finding:F2:head:end -->

<!-- sq:finding:F2:body -->
release.yml runs with 'permissions: contents: write' and uploads the VSIX release asset via 'softprops/action-gh-release@v2'. @v2 is a mutable major-version tag on a third-party action; the other actions in this workflow are exact-pinned (checkout@v6.0.3, setup-python@v6.2.0, setup-uv@v8.2.0). A third-party action that runs with write permission on releases is the highest-value supply-chain target here — pin it to an exact version or a commit SHA so a compromised/retagged @v2 can't silently gain write access at release time. Low, hardening only; not a functional defect.
<!-- sq:finding:F2:body:end -->

#### Discussion

<!-- sq:finding:F2:discussion -->
- [2026-07-17T09:32:23Z] Operator:
  - Pinned softprops/action-gh-release to the exact-SHA hardened form: @3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2 (latest v2.x release; verified via gh api tags). Scanned release.yml/vscode-client.yml/test.yml/publish.yml — no other third-party actions on mutable major tags (astral-sh/setup-uv already exact-pinned); actions/setup-node@v5 (first-party, mutable) left as-is, out of F2's third-party scope.
- [2026-07-17T09:59:07Z] Hugo Ops:
  - Superseded: publish.yml no longer uses softprops/action-gh-release at all — the VSIX job now attaches via the first-party gh CLI (gh release upload) under contents: write scoped to that job only. F2's SHA-pin no longer applies to anything; leaving status Fixed (accurate history) since the action it pinned is gone.
<!-- sq:finding:F2:discussion:end -->
<!-- sq:finding:F2:end -->

<!-- sq:finding:F3 -->
### F3 — No .vscodeignore: the VSIX will bundle src/, test/ and fixtures

<!-- sq:finding:F3:head -->
**Status:** 🟡 Fixed
**Severity:** 🟢 Low
<!-- sq:finding:F3:head:end -->

<!-- sq:finding:F3:body -->
clients/vscode/ has no .vscodeignore, so 'vsce package' will include everything not covered by defaults — src/, test/, test/fixtures/, tsconfig, eslint/prettier config — not just the compiled out/ + package.json + README. Two consequences: (1) packaging bloat (source + tests shipped to end users); (2) test/fixtures/show-raw.txt contains internal item IDs (ADR-427, TASK-428, TASK-434 — legitimate test data under the hygiene carve-out) which would then ship inside the public VSIX. Both are minor and inert, but a .vscodeignore excluding src/**, test/**, and config files is the clean fix — recommend adding one before the first real release cut. Low; TASK-433 wires the release correctly, this is packaging hygiene on top.
<!-- sq:finding:F3:body:end -->

#### Discussion

<!-- sq:finding:F3:discussion -->
- [2026-07-17T08:16:49Z] Ada Typescript:
  - Added clients/vscode/.vscodeignore excluding src/**, test/** (incl. fixtures), toolchain config, package-lock.json, and node_modules/** — VSIX now bundles only out/ + package.json + README + resources; part of TASK-440.
<!-- sq:finding:F3:discussion:end -->
<!-- sq:finding:F3:end -->
<!-- sq:findings:end -->

## Discussion

<!-- sq:discussion -->
- [2026-07-17T07:59:58Z] Paul Reviewer:
  - Closes REV-438's two design rulings at the machine surface: (a) tree titles — TASK-439 adds an additive 'title' to every sq tree --json node, so the client can drop its second sq-list-join-by-id fetch (client cutover tracked as TASK-440). Confirmed correct + additive. (b) open/closed — TASK-439 adds a spec-driven 'is_open' to sq list --json and sq tree --json, so the client can stop diffing two sq list calls (default vs --all). Confirmed spec-driven (not a hardcoded terminal set) + additive. Both rulings are the right call and now correctly implemented at the surface rather than worked around client-side.
  - (c) TASK-432 three test layers: my REV-438 F2 stands — only the unit layer exists in CI. Acceptable-with-follow-up for 0.10 (unit layer is the bulk of value, runs headless); the integration skew-canary follow-up is now tracked as TASK-441. Not re-raised here.
- [2026-07-17T09:33:29Z] Elias Python:
  - F1 fixed: sq mine --json now emits is_open per item (spec.is_open(status)), same additive pattern as list/tree; mine_manager golden regenerated (empty list, unaffected).
- [2026-07-17T11:38:08Z] Paul Reviewer:
  - F1 Fixed: sq mine --json now emits the additive, spec-driven is_open (spec.is_open(i.status)), consistent with sq list/tree --json — the un-enriched sibling surface is closed. Reviewed in REV-447 (APPROVE).
<!-- sq:discussion:end -->
