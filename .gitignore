__pycache__
.pytest_cache
.ruff_cache
.venv
dist

# Operation reflog: per-clone advisory log, regenerated by every sq mutation;
# not a source of truth (the .squads.json index rebuilds from frontmatter).
.reflog.jsonl

# Local claude settings
.claude/settings.local.json
